-r requirements.txt

# silero-vad 6.2.x imports its ONNX sequence runtime on macOS, but does not
# install this optional dependency itself. The native arm64 wheel is available.
onnxruntime>=1.23,<2

# Apple Silicon native Whisper inference used by the local OpenAI-compatible
# speech service (scripts/local_whisper_server.py).
mlx-whisper==0.4.3
